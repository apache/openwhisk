---
# Remove prereq packages

- name: remove requests
  pip: 
    name: requests
    state: absent

- name: remove docker
  pip: 
    name: docker
    state: absent

- name: remove httplib2
  pip: 
    name: httplib2
    state: absent
